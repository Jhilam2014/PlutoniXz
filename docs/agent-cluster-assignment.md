# Agent Cluster Assignment

`plutomix-fullstack-agent` owns the initial PlutoMix system. A single owner is deliberate because the first release has tightly coupled frontend/backend/runtime/container boundaries.

Future specialist agents are justified when external LLM generation, auth, persisted projects, or production deployment are added.
