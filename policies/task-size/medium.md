# Medium Task

- Use focused context for the affected feature boundary, direct dependencies, and related tests.
- A managed project may use its selected project executor; an independent reviewer runs only when separately justified.
- Update architecture-owned application artifacts only when the change crosses a real boundary.
- Prefer targeted build and integration checks over whole-workspace exploration.
