# Agent Knowledge Registry Reference

Local agent definitions are authoritative for instructions; structured records own exact scores and lifecycle; semantic memory supports bounded retrieval. Reuse precedes creation, secrets are redacted, and synchronization is content-hash idempotent. Detailed schemas remain in `full-operating-manual.md`.
