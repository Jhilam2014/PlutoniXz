# Vector Memory Reference

Local sanitized memory is the source projection. Existing idle vector synchronization runs only after local publication succeeds and within configured budgets. The model never uploads control-plane vector memory. Detailed provider and fallback procedures remain in `full-operating-manual.md`.
