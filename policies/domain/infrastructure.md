# Infrastructure

- Preserve secure sandboxing, workspace boundaries, deployment approvals, secrets, and reversible rollout.
- Distinguish runtime, container, volume, CLI, cache, provider, and project failures using structured evidence.
- Do not repair application code for infrastructure failures or replay into a partially modified unsafe workspace.
